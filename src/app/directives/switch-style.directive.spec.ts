import { SwitchStyleDirective } from './switch-style.directive';

describe('SwitchStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchStyleDirective();
    expect(directive).toBeTruthy();
  });
});
